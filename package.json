{
  "name": "absence-record",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "format": "prettier --write .",
    "db:init": "pnpm --filter @absence-record/api db:init",
    "db:seed": "pnpm --filter @absence-record/api db:seed"
  },
  "devDependencies": {
    "prettier": "^3.0.0"
  }
}
